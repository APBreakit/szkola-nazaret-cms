import type React from "react"

export const metadata = {
  robots: { index: false, follow: false },
}

export default function GroupSlugLayout({ children }: { children: React.ReactNode }) {
  return children
}

